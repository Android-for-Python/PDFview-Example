
# Pdfview does not require any changes to buildozer.spec
#
# However the example downloads a test pdf file and requires:


requirements = python3,kivy==2.0.0,requests,urllib3,chardet,idna

android.permissions = INTERNET
